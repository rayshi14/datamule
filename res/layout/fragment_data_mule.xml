<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="info.fshi.datamule.DataMule$PlaceholderFragment" >

    <TextView
        android:id="@+id/home_desc_title"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:padding="2dp"
        android:gravity="center"
        android:text="@string/app_name"
        android:textSize="50sp"
        android:textStyle="bold"/>

    <TextView
        android:id="@+id/home_desc_content"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="60sp"
        android:padding="5dp"
        android:text="@string/home_desc"
        android:textSize="20sp"/>

    <ImageView
        android:id="@+id/home_pic"
        android:layout_height="wrap_content"
        android:layout_width="fill_parent"
        android:layout_marginTop="150dp"
        android:contentDescription="@string/home_page"
        android:gravity="center"
        android:scaleType="fitCenter"
        android:src="@drawable/data_mule" />

</RelativeLayout>